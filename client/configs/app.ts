import { MetaData } from "../types/app";
import { APP_VERSION } from "./base";

export const META_DATA: MetaData = {
    version   : APP_VERSION,
    perference: {
        fullscreen: true,
        mute      : false
    },
    slots: []
};