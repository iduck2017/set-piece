/** 模型注册表 */
export enum ModelCode {
    // @model-code
    Castrated = 'castrated',
    Features = 'features',
    Deck = 'deck',
    Root = 'root',
    Timer = 'timer',
    Bunny = 'bunny',
    Card = 'card',
    Game = 'game',
    Player = 'player',
}
