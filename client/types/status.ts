enum AppStatus {
    INITED,
    UNMOUNTED,
    MOUNTING,
    MOUNTED,
    UNMOUNTING,
}

enum ModelStatus {
    INITED,
    MOUNTING,
    MOUNTED,
    UNMOUNTING,
    UNMOUNTED,
}

export {
    AppStatus,
    ModelStatus
};